package ex25;
/*
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.junit.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;
*/

public class CalculatorTest {
/*
    @Test
    @ParameterizedTest
    @DisplayName("0 + 0");
    @ValueSource(doubles = {0, 1, 1000, 10000})
    public void test_addition_set1(double arg1) {
        Calculator calculator = new Calculator();

        double actual = calculator.calculate(arg1, 0, "+");
        double expected = 0;
        assertEquals(expected, actual, 0.0001);
    }

    @Test
    @DisplayName("0 + 1")
    public void test_addition_set2() {
        Calculator calculator = new Calculator();

        double actual = calculator.calculate(Double.MAX_VALUE, Double.MAX_VALUE);
        double expected = Double.POSITIVE_INFINITY;
    }
 */
}
